name: Hugo Accessibility

on:
  workflow_call:
    inputs:
      hugo-version:
        description: 'Hugo version to use'
        type: string
        default: '0.128.0'
      extended:
        description: 'Use Hugo extended version'
        type: boolean
        default: true
      build-args:
        description: 'Additional arguments for hugo build'
        type: string
        default: '--gc --minify'
      submodules:
        description: 'Checkout submodules (true, false, or recursive)'
        type: string
        default: 'true'
      node-version:
        description: 'Node.js version'
        type: string
        default: '20'
      pa11y-config:
        description: 'Path to pa11y-ci config file'
        type: string
        default: '.pa11yci.json'
      urls:
        description: 'Space-separated list of URLs to test (relative to localhost:8080)'
        type: string
        default: '/ /about/ /blog/'
      server-port:
        description: 'Port to run local server on'
        type: string
        default: '8080'

jobs:
  accessibility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: ${{ inputs.submodules }}
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ inputs.hugo-version }}
          extended: ${{ inputs.extended }}

      - name: Build site
        run: hugo ${{ inputs.build-args }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}

      - name: Install pa11y-ci
        run: npm install -g pa11y-ci

      - name: Start local server
        run: |
          npx serve public -l ${{ inputs.server-port }} &
          sleep 5

      - name: Run accessibility tests
        run: |
          URLS=""
          for url in ${{ inputs.urls }}; do
            URLS="$URLS http://localhost:${{ inputs.server-port }}$url"
          done
          pa11y-ci --config ${{ inputs.pa11y-config }} $URLS
